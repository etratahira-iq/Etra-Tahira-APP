# معمارية المشروع — حسينية العترة الطاهرة

## 1. نظرة عامة

تطبيق ويب متكامل (Full‑Stack) يعمل كمنصة رسمية للحسينية:

- **Frontend عام** (`/`): صفحة رئيسية، محاضرات، مناسبات، معرض صور، مفقودات، تبرعات، حجز، حسابي.
- **لوحة تحكم** (`/admin`): إدارة كاملة للمحتوى والحجوزات والأسعار والفواتير والتقارير.
- **Backend REST API** (`/api/*`): Node.js HTTP مع توجيه يدوي، جلسات آمنة، RBAC.
- **قاعدة بيانات**: SQLite عبر `node:sqlite` المدمجة (بدون أي اعتماديات خارجية).

### لماذا هذا الـ Stack؟

| القرار | السبب |
|---|---|
| Node.js 22+ مع `node:sqlite` | قاعدة بيانات SQL حقيقية بدون تثبيت أي حزمة، يعمل offline، ملف واحد قابل للنسخ الاحتياطي |
| صفر اعتماديات npm | `npm start` يعمل فوراً، لا مشاكل توافق، أمان أعلى (لا supply chain) |
| Vanilla JS + ES Modules | لا خطوة بناء (build step)، تحميل فوري، أداء ممتاز على الهاتف |
| CSS مكتوب يدوياً بمتغيرات | تحكم كامل بالهوية، RTL أصلي، حجم صغير جداً |

> ملاحظة للتوسع المستقبلي: طبقة الـ API منفصلة تماماً عن الواجهة، لذا يمكن استبدال الواجهة بـ Next.js/React لاحقاً دون تغيير الـ Backend، كما يمكن استبدال SQLite بـ PostgreSQL بتعديل `server/lib/db.js` فقط.

## 2. هيكل الملفات

```
.
├── package.json
├── README.md
├── ARCHITECTURE.md
├── data/                     # ملف قاعدة البيانات (يُنشأ تلقائياً)
├── uploads/                  # الصور المرفوعة (يُنشأ تلقائياً)
├── server/
│   ├── index.js              # نقطة التشغيل + التوجيه الرئيسي
│   ├── db/
│   │   ├── schema.sql        # مخطط قاعدة البيانات الكامل
│   │   └── seed.js           # البيانات التجريبية
│   ├── lib/
│   │   ├── db.js             # اتصال قاعدة البيانات + Migrations
│   │   ├── http.js           # Router, JSON, Cookies, Static, Errors
│   │   ├── auth.js           # Hashing (scrypt), Sessions, RBAC
│   │   ├── validate.js       # التحقق من المدخلات
│   │   ├── hijri.js          # التقويم الهجري (Umm al‑Qura)
│   │   ├── uploads.js        # رفع الصور الآمن (base64 + تحقق)
│   │   ├── settings.js       # الإعدادات العامة (key/value)
│   │   ├── notify.js         # إنشاء الإشعارات
│   │   └── booking.js        # منطق الحجز: التسعير، التوفر، الحالات
│   └── routes/
│       ├── auth.js           # تسجيل/دخول/خروج/الملف الشخصي
│       ├── public.js         # محتوى عام (home, lectures, events, gallery…)
│       ├── bookings.js       # الحجز (مستخدم)
│       ├── payments.js       # العربون ووصل الدفع
│       ├── notifications.js  # إشعارات المستخدم
│       └── admin/
│           ├── index.js      # تجميع مسارات الإدارة
│           ├── stats.js      # إحصائيات + تقارير
│           ├── bookings.js   # إدارة الحجوزات والتسعير
│           ├── content.js    # محاضرات/مناسبات/معرض/مفقودات/إعلانات/تبرعات
│           ├── settings.js   # الإعدادات وطرق الدفع وروابط التواصل
│           └── users.js      # إدارة المستخدمين والصلاحيات
└── public/
    ├── index.html            # تطبيق الواجهة العامة (SPA)
    ├── admin.html            # لوحة التحكم (SPA)
    ├── css/
    │   ├── base.css          # المتغيرات، الخطوط، الأساسيات، RTL
    │   ├── app.css           # واجهة الزائر
    │   ├── admin.css         # لوحة التحكم
    │   └── print.css         # الفاتورة الحرارية 80mm
    └── js/
        ├── core/             # api, router, store, ui, format, hijri
        ├── views/            # صفحات الواجهة العامة
        └── admin/            # صفحات لوحة التحكم
```

## 3. مخطط قاعدة البيانات

### الجداول والعلاقات

```
users (1) ──< bookings (N) ──< booking_events (N)   [Timeline]
                │
                ├──< payments (N)        [العربون + وصل الدفع]
                ├──< invoices (1)        [الفاتورة]
                ├──> event_types (1)     [نوع المناسبة]
                └──> food_options (1)    [خيار الطعام]

users (1) ──< sessions (N)
users (1) ──< notifications (N)   [user_id = NULL يعني إشعار عام]

settings          (key/value)      — كل ما يمكن أن يتغير مستقبلاً
payment_methods   (دفع/تبرع)       — كي كارد، زين كاش، تحويل رصيد
social_links                       — انستغرام، فيسبوك، واتساب، اتصال
blocked_dates                      — الأيام غير المتاحة للحجز
lectures                           — المحاضرات والمجالس
events                             — المناسبات (مولود، فاتحة، ذكرى…)
gallery                            — صور الحسينية (خارج/داخل/مناسبات)
lost_found                         — المفقودات والموجودات
advertisements                     — الإعلانات
donation_campaigns                 — حملات التبرع
```

### حالات الحجز (Booking Status Machine)

| الحالة | الاسم العربي | من يغيّرها |
|---|---|---|
| `pending_review` | قيد المراجعة | النظام عند الإرسال |
| `pricing` | بانتظار تعديل السعر | المشرف |
| `awaiting_user_approval` | بانتظار موافقة المستخدم | المشرف (بعد التسعير) |
| `awaiting_deposit` | بانتظار العربون | المستخدم (عند الموافقة) |
| `deposit_submitted` | بانتظار تأكيد العربون | المستخدم (رفع الوصل) |
| `deposit_paid` | تم دفع العربون وتثبيت الحجز | المشرف (قبول العربون) |
| `confirmed` | تم التأكيد | المشرف (أو تلقائياً إذا العربون = 0) |
| `completed` | مكتمل | المشرف (بعد انتهاء المناسبة) |
| `cancelled` | ملغي | المستخدم أو المشرف |
| `rejected` | مرفوض | المشرف |

**الانتقالات المسموحة** محروسة في `server/lib/booking.js` (`canTransition`) — لا يمكن القفز بين الحالات.

### معادلة التسعير

```
subtotal   = daily_price × days
food_cost  = (خيار الطعام = مطبخ الحسينية) ? قيمة يحددها المشرف : 0
total      = subtotal + food_cost
remaining  = total − (مجموع العربونات المقبولة)
```

## 4. الأمان

- كلمات المرور: `scrypt` (N=16384) مع ملح عشوائي 16 بايت لكل مستخدم — لا تُخزَّن أبداً كنص عادي.
- الجلسات: رمز عشوائي 32 بايت، cookie بخصائص `HttpOnly; SameSite=Strict; Path=/`، صلاحية 30 يوماً، يُحذف عند الخروج.
- RBAC: `user` < `admin` < `manager`. كل مسار `/api/admin/*` محروس بـ `requireRole('admin')`.
- التحقق من المدخلات: طبقة `validate.js` على كل نقطة كتابة (نوع، طول، نطاق، صيغة).
- حماية الحجز: المستخدم لا يستطيع تعديل السعر أو الحالة أو بيانات حجز بعد الإرسال (تعديل محدود فقط في حالة `pending_review`).
- أرقام الدفع: تُقرأ من قاعدة البيانات فقط، وتُعدَّل حصرياً من لوحة التحكم — لا يوجد رقم واحد مكتوب داخل الكود.
- رفع الصور: فحص الامتداد + الـ MIME + التوقيع الثنائي (magic bytes) + حد 4MB + اسم ملف عشوائي.
- حماية من Path Traversal في الملفات الثابتة والمرفوعات.
- تحديد معدل المحاولات (Rate limiting) على تسجيل الدخول والتسجيل.

## 5. تدفق الحجز الكامل (End‑to‑End)

```
المستخدم                          النظام                           المشرف
   │                                │                                 │
   ├─ تسجيل/دخول (هاتف+كلمة مرور) ─>│                                 │
   ├─ Stepper 6 خطوات ─────────────>│                                 │
   │                                ├─ فحص التوفر والتعارض            │
   │                                ├─ إنشاء الطلب: pending_review ──>│
   │<── إشعار: تم استلام الطلب ─────┤                                 │
   │                                │<── تحديد سعر اليوم + الطعام ────┤
   │                                ├─ حساب المجموع + إنشاء فاتورة    │
   │<── إشعار: تم تحديد السعر ──────┤   awaiting_user_approval        │
   ├─ عرض الفاتورة + موافقة ───────>│                                 │
   │                                ├─ awaiting_deposit               │
   ├─ اختيار طريقة دفع + نسخ الرقم │                                 │
   ├─ رفع صورة وصل الدفع ──────────>│  deposit_submitted ────────────>│
   │                                │<── قبول / رفض / إعادة إرسال ────┤
   │<── إشعار: تم تثبيت الحجز ──────┤  deposit_paid                   │
   ├─ عرض/حفظ الفاتورة النهائية ───>│                                 │
```

## 6. التقويم الهجري

- يُحسب عبر `Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura')` المتوفر في Node والمتصفحات الحديثة.
- الواجهة تعرض **الشهر الهجري الحالي فقط** + إمكانية الانتقال إلى **الشهر القادم فقط** (نطاق شهرين كحد أقصى) كما هو مطلوب.
- كل مناسبة/محاضرة تُخزَّن بالتاريخ الميلادي (ISO) وتُعرض بالهجري والميلادي معاً.
