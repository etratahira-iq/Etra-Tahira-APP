# 🚀 إطلاق تطبيق الحسينية على Vercel (مجاناً)

الرابط النهائي سيكون بالشكل: `https://etra-tahira.vercel.app`

> **مهم:** Vercel لا يحفظ الملفات على السيرفر. لذلك التطبيق مُهيّأ ليحفظ قاعدة البيانات في
> **Upstash Redis** والصور في **Vercel Blob** — كلاهما مجاني ويُضاف من داخل Vercel بضغطتين.
> بدون هذه الخطوة ستضيع الحجوزات والحسابات والصور عند كل نشر.

---

## الخطوة 1 — رفع المشروع إلى GitHub

أنشئ مستودعاً جديداً فارغاً باسم `Etra-Tahira-APP` (بدون README ولا .gitignore ولا license)،
ثم نفّذ داخل مجلد المشروع:

```bash
git remote add origin https://github.com/etratahira-iq/Etra-Tahira-APP.git
git push -u origin main
```

> إن كان `gh` مسجّلاً بحساب آخر، بدّل الحساب أولاً: `gh auth login`

---

## الخطوة 2 — استيراد المشروع في Vercel

1. ادخل [vercel.com](https://vercel.com) وسجّل بحساب GitHub.
2. **Add New → Project** ثم اختر مستودع `Etra-Tahira-APP` واضغط **Import**.
3. اترك كل الإعدادات كما هي (Framework: **Other**) واضغط **Deploy**.
4. انتظر دقيقة — سيظهر رابط `xxxx.vercel.app`. (التطبيق لن يعمل بشكل كامل قبل الخطوة 3.)

يمكنك تغيير الاسم من: **Settings → Domains** ليصبح مثلاً `etra-tahira.vercel.app`.

---

## الخطوة 3 — قاعدة البيانات (Upstash Redis)

1. من صفحة المشروع: **Storage → Create Database → Upstash for Redis** (الخطة المجانية Free).
2. اختر أقرب منطقة (مثلاً Frankfurt) ثم **Create**.
3. اضغط **Connect to Project** واختر مشروع `Etra-Tahira-APP`.
4. تُضاف المتغيرات تلقائياً: `KV_REST_API_URL` و `KV_REST_API_TOKEN` — لا تعدّلها.

## الخطوة 4 — تخزين الصور (Vercel Blob)

1. **Storage → Create → Blob** ثم **Create a new Blob store**.
2. **Connect to Project** → يُضاف المتغير `BLOB_READ_WRITE_TOKEN` تلقائياً.

> بديل اختياري: Cloudinary — أضف `CLOUDINARY_CLOUD_NAME` و `CLOUDINARY_UPLOAD_PRESET` (Unsigned preset) بدل Blob.

بدون هذه الخطوة يعمل التطبيق، لكن رفع أي صورة (وصل دفع، صورة محاضرة، صورة مفقودات)
سيُرفض برسالة واضحة بدل أن يفشل بصمت.

## الخطوة 5 — حساب المدير وكلمة سر النسخ الاحتياطي

**Settings → Environment Variables → Add**:

| Key | Value |
|---|---|
| `ADMIN_PHONE` | رقم هاتف مدير الحسينية (مثال: `07701234567`) |
| `ADMIN_PASSWORD` | كلمة مرور قوية تختارها أنت |
| `ADMIN_NAME` | اسم المدير (اختياري) |
| `CRON_SECRET` | أي كلمة عشوائية طويلة (مثال: `hst-backup-7k29xd41`) |

> إن لم تضبط `ADMIN_PHONE` و `ADMIN_PASSWORD` سيُنشأ حساب افتراضي
> `07700000001` / `admin1234` — **غيّره فوراً** بعد أول دخول.

ثم **Deployments → آخر نشر → ⋯ → Redeploy** لتفعيل كل المتغيرات.

---

## الخطوة 6 — أول دخول بعد النشر

1. افتح `https://اسم-مشروعك.vercel.app/admin` وسجّل الدخول بحساب المدير.
2. **الإعدادات العامة:**
   - اسم الحسينية والوصف والنبذة
   - **العنوان** و**رابط Google Maps**
   - **أرقام الاتصال والواتساب**
   - حدود عدد الحضور ونسبة العربون الافتراضية
3. **طرق الدفع:** أدخل أرقام **كي كارد** و**زين كاش** و**تحويل رصيد** الحقيقية.
   (هي فارغة الآن — لا يوجد أي رقم مكتوب داخل الكود.)
4. **روابط التواصل:** إنستغرام، فيسبوك، واتساب، الاتصال.
5. **المحاضرات:** أضف أول محاضرة (اسم المحاضرة، تقديم المحاضرة، المجلس الذي يليها، اسم القارئ).
6. **معرض الصور:** ارفع صور الحسينية من الداخل والخارج.
7. **المستخدمون:** أضف المشرفات ووزّع الصلاحيات.
8. جرّب حجزاً حقيقياً من هاتفك: تسجيل → حجز → تسعير من اللوحة → موافقة → عربون → تثبيت.

---

## ما الذي يعمل تلقائياً بعد النشر؟

| الميزة | التفصيل |
|---|---|
| 🔒 HTTPS | مُفعّل تلقائياً من Vercel — لازم لتثبيت التطبيق على الهاتف |
| 📲 تثبيت التطبيق | يعمل بضغطة على أندرويد، و«إضافة إلى الشاشة الرئيسية» على آيفون |
| 🗄️ نسخة احتياطية يومية | مهمة Cron يومياً الساعة 1:00 ليلاً، ويُحتفظ بآخر 14 نسخة |
| 🛡️ حماية الدخول | 8 محاولات كل 15 دقيقة لكل رقم، و30 لكل عنوان IP |
| 🔐 كلمات المرور | مشفّرة بـ scrypt — لا تُخزَّن كنص عادي ولا تُعاد في أي استجابة |
| 📅 التقويم الهجري | يُحسب تلقائياً (أم القرى) — الشهر الحالي والقادم فقط |

---

## حدود الخطة المجانية

- **Vercel Hobby:** 100 جيجا نقل بيانات شهرياً، ومهمة Cron واحدة يومياً.
- **Upstash Free:** 10,000 أمر يومياً. التطبيق يقرأ القاعدة مرة عند بدء كل نسخة
  ويكتبها بعد كل تعديل فقط — يكفي لعشرات الحجوزات يومياً بسهولة.
- **Vercel Blob:** مساحة مجانية محدودة — صغّر الصور قبل الرفع (الحد الأقصى 4 ميغابايت للصورة).

> إذا كبر حجم قاعدة البيانات كثيراً (آلاف الحجوزات)، انتقل إلى استضافة بقرص دائم
> (Railway أو VPS) — التطبيق يعمل هناك بنفس الكود تماماً وبدون أي متغيرات بيئة.

---

## التحديث بعد أي تعديل

```bash
git add .
git commit -m "تحديث"
git push
```

ينشر Vercel النسخة الجديدة تلقائياً خلال دقيقة — والبيانات تبقى محفوظة لأنها في Redis
وليست في ملفات المشروع.

---

## النسخ الاحتياطي والاستعادة

- **يدوياً:** `POST /api/cron/backups` (للمشرف) — أو من مهمة Cron اليومية.
- **قائمة النسخ:** `GET /api/cron/backups`
- **الاستعادة:** `POST /api/cron/backups/restore` مع `{"id":"hst:backup:..."}` — **للمدير فقط**.

---

## تشغيل محلي (بدون أي إعداد)

```bash
npm run seed
```

```bash
npm start
```

بدون متغيرات بيئة يعمل التطبيق بوضع الملفات: قاعدة البيانات في `data/husseiniya.db`،
الصور في `uploads/`، والنسخ الاحتياطية في `data/backups`.
**نفس الكود يعمل في الوضعين بلا أي تغيير.**

للتأكد من أن وضع السحابة سليم قبل النشر:

```bash
node --test tests/cloud-mode.test.js
```

يشغّل هذا الاختبار خادم Redis وهمياً ويتحقق أن البيانات تبقى بعد إعادة تشغيل الخادم.
